<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Finances.App.Views.Favoreds">
    <ContentPage.Content>
        <StackLayout  Style="{StaticResource DarkTheme}">
            <Frame Style="{StaticResource BaseFrameBrief}" HeightRequest="50">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Text="Favorecidos" Style="{StaticResource LabelTitle}" VerticalOptions="Center" HorizontalOptions="Start" />
                    <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" HorizontalOptions="End" Text="Adicionar5" Command="{Binding NewFavoredCommand}"/>
                </Grid>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>